# Skip Puppeteer Chrome download due to firewall restrictions
PUPPETEER_SKIP_DOWNLOAD=true
PUPPETEER_SKIP_CHROMIUM_DOWNLOAD=true

# Network timeout settings for firewall environments
timeout=60000
registry=https://registry.npmjs.org/

# Skip optional dependencies that may have firewall issues
optional=false